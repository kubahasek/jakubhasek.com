# Agent Instructions

- Use `bun` for package management, dependency installation, scripts, and lockfile updates.
- Do not use `npm`, `pnpm`, or `yarn` in this repository.
- Prefer `bun install`, `bun add`, `bun remove`, and `bun run <script>`.
